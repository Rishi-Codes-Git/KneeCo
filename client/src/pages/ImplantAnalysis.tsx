import { KneeCoAppShell } from "@/components/KneeCoAppShell";
import { Button } from "@/components/ui/button";
import { persistedCaseToWorkspaceCase } from "@/lib/persistedCase";
import { trpc } from "@/lib/trpc";
import { ArrowRight, Box, CheckCircle2, ClipboardCheck, Layers3, LockKeyhole, Rotate3D, Ruler, Sparkles } from "lucide-react";
import { toast } from "sonner";
import { useLocation, useRoute } from "wouter";

function ThreeDPlaceholder() {
  return <div className="relative flex min-h-[330px] items-center justify-center overflow-hidden rounded-[1.5rem] border border-[#E9DDE0] bg-[radial-gradient(circle_at_50%_40%,#FDF8F9_0%,#F7ECEE_45%,#EEE1E5_100%)]"><div className="absolute inset-0 opacity-50 [background-image:linear-gradient(#CDA6AF22_1px,transparent_1px),linear-gradient(90deg,#CDA6AF22_1px,transparent_1px)] [background-size:28px_28px]" /><div className="relative flex h-40 w-40 items-center justify-center rounded-[48%_52%_44%_56%/58%_43%_57%_42%] border-[10px] border-[#B96D7E]/70 bg-[#F9E8EB] shadow-[inset_0_0_0_18px_#fff8,0_22px_36px_-22px_rgba(98,43,57,.55)]"><div className="h-20 w-20 rounded-[48%_52%_44%_56%] border-[8px] border-[#733963]/70 bg-white/70" /></div><span className="absolute left-5 top-5 flex items-center gap-2 rounded-full bg-white/80 px-3 py-1.5 text-[10px] font-extrabold uppercase tracking-[0.12em] text-[#8D5967]"><Rotate3D className="h-3.5 w-3.5" />3D planning view</span><span className="absolute bottom-5 text-xs font-semibold text-[#806C73]">Interactive rendering module ready for connection</span></div>;
}

export default function ImplantAnalysis() {
  const [, setLocation] = useLocation();
  const [, params] = useRoute("/implant-analysis/:caseId");
  const selectedReference = params?.caseId;
  const caseList = trpc.cases.list.useQuery();
  const eligibleCases = (caseList.data ?? []).map(persistedCaseToWorkspaceCase).filter((record) => record.geminiVisualReview?.presentationTestOutput?.syntheticOaStatus === "present");
  const activeReference = selectedReference ?? eligibleCases[0]?.id;
  const planning = trpc.implant.get.useQuery({ caseReference: activeReference ?? "unavailable" }, { enabled: Boolean(activeReference) });
  const utils = trpc.useUtils();
  const rank = trpc.implant.rank.useMutation({ onSuccess: () => { void planning.refetch(); toast.success("Implant reference ranking is ready for review."); }, onError: () => toast.error("Implant ranking could not be prepared.") });
  const confirm = trpc.implant.confirm.useMutation({ onSuccess: () => { void planning.refetch(); void utils.cases.list.invalidate(); toast.success("Case confirmed for clinician review."); }, onError: () => toast.error("Review the ranked candidates before confirming the case.") });
  const close = trpc.implant.close.useMutation({ onSuccess: () => { void planning.refetch(); void utils.cases.list.invalidate(); toast.success("Case closed in the workspace."); }, onError: () => toast.error("Confirm the ranked candidates before closing the case.") });
  const planningData = planning.data;
  return <KneeCoAppShell eyebrow="Planning workspace" title="Implant Analysis"><div className="mx-auto max-w-7xl"><section className="rounded-[1.75rem] border border-[#EFE4E6] bg-white p-7 shadow-[0_18px_45px_-35px_rgba(92,49,61,0.35)] sm:p-9"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-extrabold uppercase tracking-[0.17em] text-[#A6556A]">Four-dimension reference fit</p><h2 className="font-kneeco-display mt-3 text-4xl tracking-[-0.04em] text-[#352C30]">Implant Analysis</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-[#7A676E]">Review femoral AP and ML plus tibial AP and ML dimensions against the anonymous structured sizing catalogue.</p></div><span className="flex w-fit items-center gap-2 rounded-full bg-[#F8E7EB] px-3 py-2 text-xs font-extrabold text-[#934F60]"><LockKeyhole className="h-3.5 w-3.5" />Eligible planning cases only</span></div></section>{eligibleCases.length === 0 ? <section className="mt-7 rounded-[1.5rem] border border-dashed border-[#DEC9CE] bg-white p-10 text-center"><Box className="mx-auto h-8 w-8 text-[#A6556A]" /><h3 className="mt-4 text-xl font-extrabold text-[#493940]">No eligible implant analysis cases</h3><p className="mx-auto mt-2 max-w-lg text-sm leading-6 text-[#806C73]">Implant Analysis becomes available after an OA-positive assessment and all four planning dimensions are available.</p><Button type="button" onClick={() => setLocation("/cases")} className="mt-6 rounded-xl bg-[#C97C8D] text-white hover:bg-[#A6556A]">Open Case Overview</Button></section> : <><div className="mt-7 flex gap-3 overflow-x-auto pb-1">{eligibleCases.map((record) => <button key={record.id} type="button" onClick={() => setLocation(`/implant-analysis/${record.id}`)} className={`min-w-[210px] rounded-xl border px-4 py-3 text-left ${record.id === activeReference ? "border-[#C97C8D] bg-[#FFF4F6]" : "border-[#E9DEE1] bg-white"}`}><p className="text-[10px] font-extrabold uppercase tracking-[0.12em] text-[#A6556A]">Planning case</p><p className="mt-1 text-sm font-extrabold text-[#493940]">{record.patientLabel}</p><p className="mt-1 text-xs text-[#806C73]">{record.id}</p></button>)}</div><div className="mt-7 grid gap-7 xl:grid-cols-[.88fr_1.12fr]"><section className="rounded-[1.5rem] border border-[#EDE3E5] bg-white p-5 shadow-[0_18px_45px_-38px_rgba(92,49,61,.3)]"><ThreeDPlaceholder /><div className="mt-5 flex items-start gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-[#F8E7EB] text-[#A6556A]"><Layers3 className="h-4 w-4" /></span><p className="text-xs leading-5 text-[#806C73]">The current 3D planning area is a visual placeholder. The case fit review below uses the four stored planning dimensions.</p></div></section><section className="rounded-[1.5rem] border border-[#EDE3E5] bg-white p-6 shadow-[0_18px_45px_-38px_rgba(92,49,61,.3)]"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-[#A6556A]">Planning measurements</p><h3 className="mt-2 text-xl font-extrabold text-[#493940]">Reference fit inputs</h3></div><Ruler className="h-5 w-5 text-[#A6556A]" /></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[["Femoral AP", planningData?.dimensions?.femoralApMm], ["Femoral width / ML", planningData?.dimensions?.femoralWidthMm], ["Tibial AP", planningData?.dimensions?.tibialApMm], ["Tibial width / ML", planningData?.dimensions?.tibialWidthMm]].map(([label, value]) => <div key={String(label)} className="rounded-xl border border-[#EEE3E5] bg-[#FFFCFC] p-4"><p className="text-[10px] font-extrabold uppercase tracking-[0.11em] text-[#9C858C]">{label}</p><p className="mt-2 text-2xl font-extrabold text-[#493940]">{typeof value === "number" ? `${value.toFixed(1)} mm` : "—"}</p></div>)}</div>{planningData?.rankings?.length ? <><div className="mt-7 flex items-center justify-between"><div><p className="text-xs font-extrabold uppercase tracking-[0.15em] text-[#A6556A]">Ranked sizing references</p><p className="mt-1 text-xs text-[#806C73]">Sorted by four-dimension proximity.</p></div><Sparkles className="h-5 w-5 text-[#A6556A]" /></div><div className="mt-3 space-y-2">{planningData.rankings.map((candidate) => <div key={candidate.rank} className="flex items-center gap-3 rounded-xl border border-[#EEE3E5] p-3"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-[#733963] text-xs font-extrabold text-white">{candidate.rank}</span><div className="min-w-0 flex-1"><p className="text-sm font-extrabold text-[#493940]">Femoral {candidate.femoralSize} · Tibial {candidate.tibialSize}</p><p className="mt-0.5 text-xs text-[#806C73]">Dimensional proximity score {candidate.dimensionalProximityScore.toFixed(1)}%</p></div><ArrowRight className="h-4 w-4 text-[#A6556A]" /></div>)}</div><p className="mt-4 text-xs leading-5 text-[#806C73]">{planningData.summary}</p><div className="mt-6 flex flex-wrap gap-3"><Button type="button" onClick={() => activeReference && confirm.mutate({ caseReference: activeReference })} disabled={planningData.planningStatus === "confirmed" || planningData.planningStatus === "closed" || confirm.isPending} className="rounded-xl bg-[#733963] text-white hover:bg-[#5C2C4D]"><ClipboardCheck className="mr-2 h-4 w-4" />{planningData.planningStatus === "confirmed" ? "Case confirmed" : "Confirm case"}</Button><Button type="button" variant="outline" onClick={() => activeReference && close.mutate({ caseReference: activeReference })} disabled={planningData.planningStatus !== "confirmed" || close.isPending} className="rounded-xl border-[#EBCFD5] text-[#A6556A] hover:bg-[#FFF2F4]"><CheckCircle2 className="mr-2 h-4 w-4" />{planningData.planningStatus === "closed" ? "Case closed" : "Close case"}</Button></div></> : <div className="mt-7 rounded-2xl border border-dashed border-[#DEC9CE] bg-[#FFFAFB] p-6"><h4 className="font-extrabold text-[#493940]">Prepare ranked references</h4><p className="mt-2 text-sm leading-6 text-[#806C73]">Generate the ranked sizing references from the four available dimensions and the anonymized catalogue.</p><Button type="button" onClick={() => activeReference && rank.mutate({ caseReference: activeReference })} disabled={rank.isPending} className="mt-5 rounded-xl bg-[#C97C8D] text-white hover:bg-[#A6556A]">{rank.isPending ? "Preparing ranking…" : "Generate reference ranking"}</Button></div>}</section></div></>}</div></KneeCoAppShell>;
}
